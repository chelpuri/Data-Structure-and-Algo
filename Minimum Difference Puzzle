int Solution::solve(vector<int> &A, int B) {
    int n = A.size(),ans = 999999999;
    sort(A.begin(),A.end());
    for (int i=0; i<n-B+1; i++){
        if (A[i+B-1] - A[i] < ans){
            ans = A[i+B-1] - A[i];
        }
    }
    return ans;
}
